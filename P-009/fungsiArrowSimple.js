// contoh arraw function sederhana
const sapa = (nama) => {
  console.log(`Halo, ${nama}!`);
};
sapa("Bayu");

// penulisan singkat jika hanya return
const tambah = (a, b) => a + b;
console.log(tambah(3, 5)); // Output: 8
